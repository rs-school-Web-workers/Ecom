export enum errorExistingEmail {
  errorMsg = 'There is already an existing customer with the provided email.',
}

export enum errorInvalidJSON {
  errorMsg = 'Request body does not contain valid JSON.',
  showMsg = 'Check the entered data. Perhaps you forgot something and made a mistake.',
}
